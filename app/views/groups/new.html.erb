<%= render partial: 'shared/header', locals: { heading: 'ADD NEW CATEGORY', path: groups_path } %>

<%= render 'shared/flash' %>

<%= form_with model: @group, url: groups_path, local: true, class: 'form' do |f| %>
  <%= f.text_field :name, autofocus: true, placeholder: "Category Name", class: "field" %>
  <div class="checkbox-btn">
    <%= f.file_field :icon %>
    <%= f.submit "Save", class: "welcome-login btn" %>
  </div>
<% end %>